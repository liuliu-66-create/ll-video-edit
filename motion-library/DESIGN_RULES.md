# 视频动效素材库设计与定位规则

## 0. 强制居中验收规则

- 数字、关键词和标签文字必须以承载它们的真实贴纸为参照居中，禁止以画布、整组元素或相邻贴纸为参照。
- 同一模板包含多种底纸外观时，必须逐张测量并登记内容安全区；不得只验证第一张，再假定后续底纸相同。
- 同一组中的 `01、02、03……` 必须逐项打开辅助框检查水平中心和垂直中心，不能只检查第一项。
- 关键词同样必须逐项检查，不得出现第一项居中、后续项目逐渐上移、下移、左偏或右偏。
- 文字变长时只能缩小字号或按模板规则换行，绝不能通过移动中心点解决。
- 标准数量与最大数量的静态检查图都通过后，模板才能写入 `library.json` 的 `validatedVariants`。
- 任何一项出现偏心，整个模板均视为未通过，不得交付、登记或被 AI 自动调用。

## 1. 视觉基线

- 画面比例：16:9，默认 1920×1080。
- 主风格：灰白旧报纸、真实撕纸纤维、低饱和砖红点缀。
- 禁止：高亮描边、发光、过度偏黄、规则 UI 卡片、无意义横线。
- 素材库模板不包含账号 Logo；Logo 在完整成片最外层统一添加。

## 2. 图层职责

- 固定层：旧报纸背景和不会变化的底纹。
- 素材层：撕纸、衬纸、印章、箭头和插画。
- 内容层：文字、数字、图片和录屏，必须可以替换。
- 需要独立运动或替换的元素必须独立成层。

## 3. 定位规则

- 每个不规则素材必须登记真实锚点，不能靠目测填写大概坐标；相似外观也必须逐项验证。
- 序号使用圆章真实圆心定位，并在等大的透明容器中双向居中。
- 多点列举模板的文字中心必须锁定在整张米白撕纸的视觉中心；左侧圆章不参与文字中心计算。
- 圆章和固定序号视为一个完整贴纸元素，一起定位、一起运动，不允许分别漂移。
- 长文字只允许自动缩小字号，不允许改变中心点，也不得逐句手工调整位置。
- 调试状态必须显示圆章圆心和文字安全区，正式导出时关闭辅助线。
- 流程步骤的文字必须放在真实纤维撕纸标签上，不得用规则色块或程序矩形代替。
- 流程连接必须使用带磨损、刮痕和不规则纸边的砖红纸质箭头，不得退化为平滑矢量线。
- 箭头、节点和标签按同一路线共同排布；改变步骤数量时只调整路线和间距，不改变已经确认的材质语言。

## 4. 数量适配

- 2～3点：单列大纸条。
- 4点：单列中等纸条。
- 5点：单列紧凑纸条。
- 6点：两列三行，避免文字过小。
- 序号由项目顺序自动生成；撕纸外观可以循环，但锚点必须跟随对应外观。

## 5. 模板完成标准

一个模板只有同时满足以下条件才可登记为可用：

1. 静态效果图已确认。
2. 固定层、素材层、内容层已拆分。
3. 可替换内容已经参数化。
4. 至少检查标准数量和最大数量。
5. 无文字溢出、序号偏心、元素遮挡和字幕安全区冲突。
6. 只保留一个对外预览样片，其余数量仅作为内部检查。
7. 已创建该模板独立的 `TEMPLATE.md` 和 `layout.json`，并由 `library.json` 指向这两个文件。

未满足以上任一项，模板只能处于制作或检查状态，不能开始制作下一个模板。

## 6. 新增模板流程

1. 明确常用场景。
2. 生成并确认一张静态效果图。
3. 拆分一次公共素材。
4. 登记锚点和内容安全区。
5. 制作可替换内容和入场动效。
6. 写完该模板的 `TEMPLATE.md` 与 `layout.json`。
7. 完成边界检查后登记到 `library.json`，状态标记为 `validated`。
